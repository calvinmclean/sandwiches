---
version: '3'

services:

  recipes:
    build:
      context: '.'
      args:
        app: 'recipes'

  ingredients:
    build:
      context: '.'
      args:
        app: 'ingredients'

  menu:
    build:
      context: '.'
      args:
        app: 'menu'
    depends_on:
      - 'recipes'
      - 'ingredients'

  client:
    build:
      context: '.'
      args:
        app: 'client'
    depends_on:
      - 'recipes'
      - 'ingredients'
      - 'menu'
    ports:
      - '80:8080'
